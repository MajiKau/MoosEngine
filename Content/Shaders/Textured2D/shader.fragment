#version 430
in vec2 tex_coord;
uniform sampler2D texture;
uniform vec4 colour;
out vec4 frag_colour;

void main() 
{
	frag_colour = colour*texture2D(texture, tex_coord);
	if(frag_colour.a < 0.01)
	{
		discard;
	}
};