#include "Vertex.h"

/*Vertex operator+(const Vertex& lhs, const Vertex& rhs)
{
	return Vertex(lhs.x + rhs.x, lhs.y + rhs.y, lhs.z);
}

Vertex RotateVertex(Vertex vertex, float angle)
{
	Vertex res;
	res.z = vertex.z;
	res.x = cosf(angle)*vertex.x - sinf(angle)*vertex.y;
	res.y = sinf(angle)*vertex.x + cosf(angle)*vertex.y;
	return res;
}*/
